version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install && npm run build
    build:
      commands:
        - npm run start:prod
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
